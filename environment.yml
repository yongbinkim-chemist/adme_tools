# conda env create -f environment.yml
# conda remove --name my-rhino-env --all

name: my-rhino-env
channels:
  - pytorch
  - conda-forge
  - defaults

variables:
  KMP_DUPLICATE_LIB_OK: "TRUE"
  OMP_NUM_THREADS: "1"

dependencies:
  - python=3.10
  - pandas=2.1.4
  - pip
  - rdkit
  - pytorch
  - torchvision
  - torchaudio
  - numpy
  - scikit-learn
  - matplotlib
  - joblib
  - tqdm
  - pip:
      - PyTDC
      - chemprop
      - lightning
      - tensorboard
